# VeriFi Final Poster

This folder is the commit-ready poster bundle.

Build from this directory with:

```sh
TEXMFHOME=../texmf TEXMFVAR=../texmf-var TEXMFCACHE=../texmf-var lualatex -interaction nonstopmode poster.tex
```

The LaTeX source expects its assets under `images/` and `logos/`.
